# order_to_blob
```
A script that observes a JSON stream (from azure iot hub) containing work orders,
captures the start and end of the stream and save the record to azure blob storage

The details on the storage and iot hub should be added to .env
```
